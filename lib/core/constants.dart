import 'package:flutter/material.dart';

/// Centralised colour tokens used across the whole app.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF001F3F);
  static const Color accent = Color(0xFF0074D9);
  static const Color background = Color(0xFFF9FAFB);
  static const Color darkBackground = Color(0xFF1A202C);
  static const Color teal = Color(0xFF0EA5A4);
  static const Color dark = Color(0xFF0F172A);
}
